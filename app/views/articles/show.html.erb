<div class="row">
  <div class="span8 offset1">
    <div><h2><%= @article.title %></h2></div>
    <div><%= @article.content %></div>
    <% if user_signed_in? && current_user.articles.include?(@article) %>
      <%= link_to 'Edit Article', edit_article_path %>
    <% end %>
  </div>
</div>